# ============================================================
# PyPostgres Configuration File
# ============================================================
# Copy this file to .env and update with your credentials
# DO NOT commit .env to version control (it's in .gitignore)
# ============================================================

# ============================================================
# PostgreSQL Database Configuration
# ============================================================
# Update these values with your PostgreSQL credentials
# For local development, default values often work
# For production, use secure credentials and strong passwords

# Database host/server
# Examples: localhost, db.example.com, 127.0.0.1
DB_HOST=localhost

# Database port (PostgreSQL default is 5432)
DB_PORT=5432

# Database name
# TODO: Replace 'your_database' with your actual database name
# Example: my_app_db, production_db, development_db
DB_NAME=your_database

# Database user/role
# TODO: Replace 'your_user' with your PostgreSQL username
# Example: postgres, app_user, myapp
DB_USER=your_user

# Database password
# TODO: Replace 'your_password' with your PostgreSQL password
# IMPORTANT: Keep this secure in production!
# Never use default/weak passwords in production
DB_PASSWORD=your_password

# ============================================================
# Application Logging Settings
# ============================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG: Very detailed information (development)
# INFO: General information messages (default)
# WARNING: Warning messages
# ERROR: Error messages
# CRITICAL: Critical errors only (production)
LOG_LEVEL=INFO

# Log file location
# Logs will be written to this file
# Directory must exist (created automatically on first run)
LOG_FILE=logs/app.log

# ============================================================
# Optional Settings (Advanced)
# ============================================================

# Maximum batch size for insert operations
# Increase for better performance on large inserts
# Decrease if memory is limited
# Default: 1000
# MAX_BATCH_SIZE=1000

# Connection timeout in seconds
# How long to wait before timing out a connection attempt
# Default: 30
# TIMEOUT=30

# ============================================================
# SETUP INSTRUCTIONS
# ============================================================
# 1. Open this file in a text editor
# 2. Replace values with YOUR database credentials:
#    - DB_HOST: Your PostgreSQL server address
#    - DB_PORT: Your PostgreSQL port (usually 5432)
#    - DB_NAME: Name of your database
#    - DB_USER: Your PostgreSQL username
#    - DB_PASSWORD: Your PostgreSQL password
# 3. Save as .env (same directory as setup.py)
# 4. Test with: python3 -c "from src.postgres_manager import PostgresManager; PostgresManager().connect()"
#
# EXAMPLE CONFIGURATIONS:
#
# Local Development (macOS/Linux with default PostgreSQL):
#   DB_HOST=localhost
#   DB_PORT=5432
#   DB_NAME=myapp_dev
#   DB_USER=postgres
#   DB_PASSWORD=postgres
#   LOG_LEVEL=DEBUG
#
# Local Development (Windows with PostgreSQL):
#   DB_HOST=localhost
#   DB_PORT=5432
#   DB_NAME=myapp_dev
#   DB_USER=postgres
#   DB_PASSWORD=<your_postgres_password>
#   LOG_LEVEL=DEBUG
#
# Production (Remote Server):
#   DB_HOST=db.production.example.com
#   DB_PORT=5432
#   DB_NAME=prod_database
#   DB_USER=prod_user
#   DB_PASSWORD=<strong_secure_password>
#   LOG_LEVEL=WARNING
#
# Docker Environment:
#   DB_HOST=postgres  (container name)
#   DB_PORT=5432
#   DB_NAME=postgres
#   DB_USER=postgres
#   DB_PASSWORD=postgres
#
# ============================================================
